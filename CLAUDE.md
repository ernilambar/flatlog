# flatlog

## Quality Gate

Run before committing or considering any code change done:

```bash
npm run lint
```

Auto-fix style issues with:

```bash
npm run lint:fix
```

All code must pass `standard` with zero errors before shipping.
